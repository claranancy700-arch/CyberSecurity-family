# Copy to .env and fill in values (never commit .env)

# Optional: Neon Postgres connection string (pooled recommended)
# DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Admin access key for dashboard API (must match what you type in admin login)
ADMIN_KEY=CTF-Admin-2026

# Local API port
PORT=8787
